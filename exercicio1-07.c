/* 2o programa da pagina 16 */
#include <stdio.h>
/* copia entrada para saida; 2a. versao */
main()
{
	printf("%d\n", EOF);
}

/* O EOF eh uma constante definida na bilbioteca stdio.h que 
 * guarda o seguinte valor: -1
 * EOF eh apenas uma forma mais amigavel de mostrar para quem 
 * estah lendo o codigo, que algo acontece quando uma funcao 
 * le um final de arquivo. 
 */
